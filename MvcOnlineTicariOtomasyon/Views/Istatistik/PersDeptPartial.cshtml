@model IEnumerable<MvcOnlineTicariOtomasyon.Models.PartialClasses.SinifGrup2>

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Departman Personel Sayıları</h3>
    </div> 
    <div class="card-body p-0">
        <table class="table table-sm">
            <thead>
                <tr>
                    <th style="width: 10px">#</th>
                    <th>Departman</th>
                    <th>Progress Bar</th>
                    <th style="width: 40px">Personel Sayısı</th>
                </tr>
            </thead>
            <tbody>
                @{ int siraNo = 0;}
                @foreach (var x in Model)
                {
                    <tr>
                        <td>@{siraNo++;} @siraNo</td>
                        <td>@x.DepartmanAdi</td>
                        <td>
                            @if (x.Sayi == 1)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar bg-success" style="width: 25%"></div>
                                </div>
                            }
                            @if (x.Sayi == 2)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar  bg-warning" style="width: 55%"></div>
                                </div>
                            }
                            @if (x.Sayi > 2)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar bg-danger" style="width: 75%"></div>
                                </div>
                            }
                        </td>
                        @if (x.Sayi == 1)
                        {
                            <td><span class="badge bg-success">@x.Sayi</span></td>
                        }
                        @if (x.Sayi == 2)
                        {
                            <td><span class="badge bg-warning">@x.Sayi</span></td>
                        }
                        @if (x.Sayi > 2)
                        {
                            <td><span class="badge bg-danger">@x.Sayi</span></td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div> 
</div> 